# profile — пример конфига
# Скопировать в .env.profile и заполнить.

PROFILE_TOKEN=change-me
PROFILE_ADMIN_TOKEN=change-me-admin

# Директория для SQLite БД (profile.db)
DB_DIR=/db

# Если true — при старте автоматически загружает fixtures/example_account.json
BOOTSTRAP_EXAMPLE=false
