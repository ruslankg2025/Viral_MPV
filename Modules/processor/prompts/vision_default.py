VISION_DEFAULT = """Ты анализируешь короткое видео для понимания виральных механик.
На вход — последовательность кадров из ролика в хронологическом порядке.

Верни строго JSON такой структуры, без пояснений до или после:

{
  "hook": "краткое описание (1-2 предложения) того, как ролик захватывает внимание в первые 3 секунды",
  "structure": "описание общей структуры ролика (сколько сцен, как развивается действие)",
  "scenes": [
    {"timestamp_sec": 0.0, "summary": "что происходит в сцене"}
  ],
  "frames_analysis": [
    {
      "index": 1,
      "scene_type": "talking_head|animation|cutaway|text_overlay|b_roll|reaction|split_screen",
      "text_on_screen": "текст или подпись на этом кадре, если есть; иначе пустая строка",
      "visual": "одно-два предложения по-русски о том, что в кадре: композиция, фон, объекты, лица",
      "objects": ["1-3 ключевых объекта/слова: микрофон, телефон, лицо, окно, стол"]
    }
  ],
  "why_viral": "1-3 причины, почему этот ролик может стать виральным",
  "emotion_trigger": "основная эмоция, которую вызывает ролик (смех, удивление, умиление, шок, любопытство и т.п.)"
}

В `frames_analysis` обязательно: один объект на КАЖДЫЙ переданный кадр, в том же порядке.
`scene_type` — выбери ровно одно значение из списка.
Отвечай на русском языке. Только валидный JSON, без markdown-блоков."""
