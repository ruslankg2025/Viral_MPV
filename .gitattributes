* text=auto

# Shell/unix scripts — обязательно LF, иначе bash на Linux не запустит.
*.sh        text eol=lf
*.yml       text eol=lf
*.yaml      text eol=lf
Caddyfile   text eol=lf
Dockerfile  text eol=lf
