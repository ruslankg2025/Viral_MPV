# Knowledge service — RAG для VIRA (этап 4 self-learning agent)

# Auth-токен для входящих запросов от shell-сервиса
KNOWLEDGE_TOKEN=dev-knowledge-token-change-me

# OpenAI API ключ для embeddings (text-embedding-3-small)
# $0.02/1M токенов — копейки. Без ключа /upload и /query вернут 503.
OPENAI_API_KEY=

DB_DIR=/db
